base_branch: main
worktree_dir: ../wt-trees

tasks:
  - id: feat-auth
    prompt: "Implement OAuth2 authentication"
    agent: echo "Running auth task..."

  - id: feat-payments
    prompt: "Add Stripe integration"
    agent: echo "Running payments task..."

merge_strategy: squash
cleanup: auto
